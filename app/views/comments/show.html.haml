- present @comment do |comment_presenter|
  = render layout: 'comments/comment', locals: { comment: @comment } do
    - unless @new_comment.text.blank?
      = render @new_comment
    = comment_presenter.replay_form(@new_comment)
