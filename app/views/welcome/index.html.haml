= link_to t('posts.new'), new_post_path
%table
  - @posts.each do |post|
    %tr
      %td
        = post.author.email
      %td
        = post.title
      %td
        = link_to t('show'), post
      %td
        = link_to t('edit'), edit_post_path(post)
      %td
        = link_to t('destroy'), post, :method => :delete, :confirm => t('are_you_sure')
    

